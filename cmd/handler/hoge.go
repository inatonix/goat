package handler

func SearchHandlers(ls string) (string, error) {
	// ll := strings.Split(string(ls), "\n")
	// for _, name := range ll {
	// 	if name == "handler" || name == "controller" {

	// 	}
	// }
	// fmt.Println(ll[0])
	// return "", nil
}
